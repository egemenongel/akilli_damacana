class ApiConstants {
  static const baseUrl = "https://task.bixos.io/api/v1/";
  static const login = "${baseUrl}auth/login";
  static const productsList = "${baseUrl}products";
  static const orderCreate = "${baseUrl}order";
  static const profile = "${baseUrl}profile";
  static const profilePictureEdit = "${baseUrl}profile/picture";
}
